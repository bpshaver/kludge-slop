---
target-branch: <branch this feature's PRs merge into>
issue: <url, if there is one>
approved: <empty until a human approves this overview — see README.md>
---

# <feature name>

<Intent of the feature, its scope, and any constraint that spans slugs. Written
by the human (or an orchestrator agent working with the human) — workers
propose changes under a `## Proposed overview changes` heading in their own
slug doc rather than editing this file directly.>

<**No slug is written into this workspace until `approved:` is set.** Until
then this file is a draft under discussion, and the phase directories stay
empty.>
